import React from 'react';

import Container from 'react-bootstrap/Container';

export const ProcessingExport = () => {
    return (
        <div style={{height: '100vh', background: 'black', 'padding-top': '4em'}} id='processing-export'>
            <Container fluid>

                <h2 style={{color: 'white'}}>Processing & Export Section</h2>

            </Container>
        </div>
    );
}

export default ProcessingExport;